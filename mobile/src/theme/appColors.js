/**
 * App-level surfaces (re-export design tokens for convenience).
 * Prefer importing from `./tokens` for new code.
 */
export { appBackground, background, backgroundBrandDark, primaryOnDark, primary, secondary } from "./tokens";
