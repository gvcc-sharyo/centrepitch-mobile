# Landing assets (drop-in folder)

Put landing page media here (copied from `frontend/public/` when available), for example:

- `sports.mp4`
- `bento_events_bg.png`
- `bento_academy_bg.png`
- `bento_courts_bg.png`
- `cta_bg.png`
- `faq_top_card_bg.png`
- `host_img.png`
- `coach_img.png`
- `academy_img.png`

Once these files exist, we’ll reference them in React Native via `require(...)` (or `ImageBackground`) so they bundle correctly in Expo.

